export enum ApiStatus {
  INIT,
  LOADING,
  SUCCESS,
  ERROR,
}
export enum UserType {
  APPLICANT,
  AGENCY_USER,
}
export enum CVEntryStatus {
  INIT,
  IN_PROGRESS,
  SUBMITTED,
  LOADING,
  COMPLETED,
  ERROR,
}
export enum CVStatus {
  INIT,
  LOADING,
  SUCCESS,
  ERROR,
}
